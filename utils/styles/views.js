import { StyleSheet } from 'react-native'

export const views = StyleSheet.create({
    center: {
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
        marginRight: '5%',
        marginLeft: '5%'
    }
})