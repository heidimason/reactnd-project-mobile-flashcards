import { StyleSheet } from 'react-native'

export const text = StyleSheet.create({
    deckTitle: {
        fontWeight: 'bold',
        textAlign: 'center'
    },
    numCards: {
    	marginTop: '1%',
    }
})